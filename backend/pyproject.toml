[project]
name = "todo-backend"
version = "0.1.0"
description = "Simple FastAPI ToDo backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
    "alembic",
    "pydantic",
]
